

export const getActiveChatsUsers = async (req,res) => {
    //want to fetch all users which match current user id in either user1 or user2
    //fetch users in the activechats array in user db.
    //TO DO - add activechats properly to user collection
}